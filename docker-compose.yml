services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant_new
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_storage:/qdrant/storage
    networks:
      - qdrant_net

networks:
  qdrant_net:
    driver: bridge
